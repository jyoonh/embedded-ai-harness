.claude/scripts/validate.py를 실행하여 하네스 동기화 갭을 검사한다.

다음 명령을 실행하고 결과를 그대로 출력한다:

```bash
cd /workspace/fisheye_detection/embedded-ai-harness && python .claude/scripts/validate.py --git-diff
```

실패 항목이 있으면 각 항목을 수정하는 방법을 간략히 안내한다.
